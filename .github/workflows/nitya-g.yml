name: Nitya_G (Database & Monitoring)

on:
  push:
    branches: [ main ]

jobs:
  monitor:
    runs-on: ubuntu-latest
    steps:
      - name: Database Health Check
        run: echo "Nitya verified MongoDB and Grafana sync."
